spring:
  application:
    name: chat-club-oss
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 47.92.163.88:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
